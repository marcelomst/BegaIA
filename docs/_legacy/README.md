# Legacy / Deprecated

Esta carpeta contiene documentación histórica, borradores y artefactos no curados.

Reglas:

- Si un documento sigue siendo relevante, se migra a `docs/architecture/`, `docs/product/` o `docs/development/`.
- No se mantiene esta carpeta ordenada: funciona como “archivo histórico”.
