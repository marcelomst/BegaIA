# Imagen de desarrollo para la app web (Next/Vite/etc.) con hot reload.
FROM node:20-alpine

WORKDIR /app

# PNPM preparado vía Corepack
RUN corepack enable && corepack prepare pnpm@10.12.3 --activate

# Instalar deps con cache de capas
COPY package.json pnpm-lock.yaml ./
RUN pnpm install

# En DEV montamos el código con volumes (compose), por eso NO copiamos el proyecto aquí.
# Si preferís que también funcione sin volumen, descomenta:
# COPY . .

EXPOSE 3000
ENV NODE_ENV=development

# Correr el server de desarrollo escuchando en 0.0.0.0 (accesible desde host/otros contenedores)
CMD ["pnpm", "dev", "-p", "3000", "-H", "0.0.0.0"]
